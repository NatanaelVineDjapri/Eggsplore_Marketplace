import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.orange;               // warna utama (teks, ikon)
  static const backgroundCard = Color(0xFFFFF9C4);   // kuning lembut untuk card
  static const iconBg = Color(0x33FFA500);       
  static const white = Colors.white;
  static const grey = Colors.grey;
}
