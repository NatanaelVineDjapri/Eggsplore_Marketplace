import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.orange;
  static const backgroundCard = Color(0xFFFFF9C4);
  static const iconBg = Color(0x33FFA500);
  static const white = Colors.white;
  static const grey = Colors.grey;
  static const bleki = Colors.black;
  static const blu = Colors.blue;
  static const amber = Colors.amber;
  static const redd = Colors.red;
}
